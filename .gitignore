# Project ignore rules
# - Ignore build artifacts anywhere (dist/, build/) to avoid committing compiled assets
# - Ignore src/dist/ because Vercel builds from the src directory and outputs to src/dist
# - Keep environment files out of version control

# Node
node_modules/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build outputs
dist/
build/
src/dist/
*.zip

# Local env
.env
.env.*
